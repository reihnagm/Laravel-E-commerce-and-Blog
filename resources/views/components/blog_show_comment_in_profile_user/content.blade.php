{{-- @if($blog)
 <comment :blogid="{{ $blog->id ? $blog->id : null  }}" 
          :bloguserid="{{ $blog->user->id ? $blog->user->id : null }}" 
          :userid="{{ Auth::user()->id ? Auth::user()->id : null }}">
 </comment>
 @else
 @endif --}}
